version: "3.8"

<<<<<<< HEAD
services:

=======
# ─────────────────────────────────────────────────────────────────────────────
# Navi — Full Harness Test Environment
#
# Brings up the complete test infrastructure described in HARNESS.md:
#   - PostgreSQL 15 (ephemeral, navimvp_test DB)
#   - OpenAI stub server (tests/stubs/openai_stub.py on port 11435)
#   - Test runner (pytest against the SUT pointed at the stub)
#
# Usage:
#   docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit
#
# Exit code of `test-runner` container is the exit code of the entire suite.
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ─── Test database ───────────────────────────────────────────────────────
>>>>>>> bfa62b0 (Add Obsidian workspace and AI architecture files)
  test-db:
    image: postgres:15
    environment:
      POSTGRES_USER: navimvp
      POSTGRES_PASSWORD: navimvppw
      POSTGRES_DB: navimvp_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U navimvp -d navimvp_test"]
      interval: 3s
      timeout: 5s
      retries: 15
    tmpfs:
<<<<<<< HEAD
=======
      # Use tmpfs so the test DB lives in memory — faster and guaranteed clean
>>>>>>> bfa62b0 (Add Obsidian workspace and AI architecture files)
      - /var/lib/postgresql/data
    networks:
      - test-net

<<<<<<< HEAD
=======
  # ─── OpenAI stub server ──────────────────────────────────────────────────
>>>>>>> bfa62b0 (Add Obsidian workspace and AI architecture files)
  openai-stub:
    build:
      context: .
      dockerfile: tests/stubs/Dockerfile.openai_stub
    ports:
      - "11435:11435"
    environment:
      PORT: 11435
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:11435/stub/health || exit 1"]
      interval: 3s
      timeout: 5s
      retries: 10
    networks:
      - test-net

<<<<<<< HEAD
=======
  # ─── Test runner ─────────────────────────────────────────────────────────
>>>>>>> bfa62b0 (Add Obsidian workspace and AI architecture files)
  test-runner:
    build:
      context: .
      dockerfile: tests/stubs/Dockerfile.test_runner
    depends_on:
      test-db:
        condition: service_healthy
      openai-stub:
        condition: service_healthy
    environment:
<<<<<<< HEAD
=======
      # Database — points to the ephemeral test DB container
>>>>>>> bfa62b0 (Add Obsidian workspace and AI architecture files)
      DATABASE_HOST: test-db
      DATABASE_PORT: 5432
      DATABASE_NAME: navimvp_test
      DATABASE_USER: navimvp
      DATABASE_PASSWORD: navimvppw
<<<<<<< HEAD
      SECRET_KEY: harness-test-secret-key
      ADMIN_SECRET_KEY: harness-admin-secret-key
      ALGORITHM: HS256
      OPENAI_API_KEY: sk-stub-key-harness
      OPENAI_BASE_URL: http://openai-stub:11435/v1
      ACCOUNT_SID: test-account-sid
      AUTH_TOKEN: test-auth-token
      TWILIO_NUMBER: "whatsapp:+5511999999999"
=======

      # Auth — test-only secrets, never reused in production
      SECRET_KEY: harness-test-secret-key
      ADMIN_SECRET_KEY: harness-admin-secret-key
      ALGORITHM: HS256

      # OpenAI — points to the stub server (not real OpenAI)
      OPENAI_API_KEY: sk-stub-key-harness
      OPENAI_BASE_URL: http://openai-stub:11435/v1

      # Twilio — test values (no real messages sent)
      ACCOUNT_SID: test-account-sid
      AUTH_TOKEN: test-auth-token
      TWILIO_NUMBER: "whatsapp:+5511999999999"

      # App
>>>>>>> bfa62b0 (Add Obsidian workspace and AI architecture files)
      APP_ENV: test
      APP_DEBUG: "false"
    command: >
      pytest tests/
        -v
        --tb=short
        --cov=app
        --cov-report=term-missing
        --cov-report=xml:/reports/coverage.xml
        --junitxml=/reports/test-results.xml
    volumes:
      - ./reports:/reports
    networks:
      - test-net

networks:
  test-net:
    driver: bridge
